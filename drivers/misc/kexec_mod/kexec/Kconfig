config KEXEC_LOAD
	tristate "kexec module"
	depends on KEXEC_MODULE && PROCFS_RW && !KEXEC
	default m
	help
	  This option create a kexec module.

	  To compile this driver as a module, choose M here: the module will
	  be called kexec_load.
